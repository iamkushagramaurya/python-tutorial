import os
import shutil
# os.walk("")  #check all the folder and folders and files inside folders
# os.makedirs() # used to create folders  inside folder
# shutil.rmtree("") #used to delete folders which contains files
# shutil.copytree(" from "," to/from") # used to copy folders from on path to another path 
# shutil.copy('new ','to/new') # used to copy files
# shutil.move('new ','to/new') # used to move files and folders
